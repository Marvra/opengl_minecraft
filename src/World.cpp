#include "World.h"

void World::generateChunks()
{
    for(int x  = 0; x < 4; x++)
    {
        for (int y = 0; y < 4; y++)
        {
            //chunks.try_emplace({x,y},Chunk());
        }
        
    }
}